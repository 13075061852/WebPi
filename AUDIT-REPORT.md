# WebPi 全面排查与性能优化报告

日期：2026-09-11

## 结果

已完成代码审查、针对性缺陷修复、独立回归、真实 Electron 界面测试、生产依赖检查和 Windows 免安装构建。最终统一回归 **28/28 通过**；check 覆盖 120 个文件，lint 通过。

## 修复与优化

| 范围 | 问题与改进 | 验证 |
| --- | --- | --- |
| 长会话 | 恢复历史时逐条读取滚动高度造成重复布局，改为恢复完成后统一滚动 | 200 轮/400 条消息由 7729ms 降到最终复测 589ms；最大帧间隔 848ms 降到 57ms；完整恢复 200 轮 |
| 思考流式文本 | 每个 delta 都解析和布局，改为同帧合并，结束时刷新尾部 | 100 个 delta 一次渲染，末尾文本不丢失 |
| 文件预览 | 旧异步读取覆盖新文件；大文件同步读取阻塞主进程 | 请求序号防过期覆盖；异步限量读取；20 个并发大文件、Unicode 截断、空文件和二进制测试 |
| SSH 连接 | 多命令重复连接；删除时未完成连接可能继续建立 | 6 个并发命令仅一条连接，删除取消握手，错误结束等待 |
| 远程命令 | 取消可能误报成功、跨包中文乱码、错误时计时器残留 | 统一完成清理、UTF-8 解码、截断提示、迟到通道关闭 |
| 端口预览 | 探测中断开未立即清理，重复请求创建资源 | 规范化端口请求合并、探测取消、监听和隧道销毁测试 |
| 会话与项目 | 部分切换和删除操作未串行，目录失效仍尝试改状态 | 共用队列；失败不阻塞后续操作；无效项目不改配置 |
| 文件边界 | 字符串路径比较可被目录链接绕过；Windows 大小写根目录校验不一致 | 校验真实路径，拒绝外部 junction，兼容项目别名和大小写 |
| IPC | 主进程接口没有统一来源验证 | 校验窗口、主 frame、准确文档 URL；拒绝外部页面和子 frame |
| 设备交互 | 手机底部横条关闭后残留 | 样式生命周期清理；设备反复切换、视口一致、圆点拖动、关闭清理 |
| 外观回归 | 为最近新增主题/透明度补充持续回归 | 8 个图片主题、10 张缩略图同尺寸、透明度三档变化和重启保存 |
| 打包 | 可选原生模块重复编译导致缺 Visual Studio 时构建失败 | 使用已提供的 Windows x64 Node-API 预编译模块，保留 asarUnpack，构建及打包终端验证 |

## 测试记录

- `npm run test:audit`：28 项全部通过。包含真实本地 SSH 服务、生命周期/边界测试及 7 项 Electron E2E。
- `npm run lint`、`git diff --check`：通过。
- `npm audit --omit=dev --json`：本次查询生产依赖已知漏洞数 0。
- `npm run dist:dir`：最新代码免安装构建成功。
- 终端 E2E：独立 shell、输入输出、分屏、隐藏恢复、关闭隔离，18,000 行输出与 Ctrl+C 环境保留。
- 消息与历史：图片输入/预览、工具状态恢复、流式续写、会话切换隔离、明暗文档渲染通过。

详细逐项耗时及结果：`test/results/audit.json`。逐项控制台输出：`test/results/*.log`。全部过程记录：`AUDIT-PROGRESS.md`。

## 验证范围

性能数字来自本机固定测试场景，不能视为所有机器的耗时保证。本次主要使用隔离配置、模拟模型事件和真实本地 SSH 服务验证；没有改动生产服务器业务，没有针对每个外部模型供应商重新发起付费请求，也没有替所有线上站点执行登录后的业务操作。

Windows 免安装程序在 `dist/win-unpacked/Pi Halo.exe`。程序仍依赖本机全局 pi SDK；该依赖与原有架构一致。打包后的 SDK 登录、供应商网络及服务端网站业务由各自环境决定。

额度本轮检查最低仍约剩余 12%，未达到用户要求的剩余 1% 重置条件，未消耗重置卡。
