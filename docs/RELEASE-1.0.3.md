安装器现在默认展示实际执行阶段、当前操作、累计耗时和安装记录。九个阶段覆盖环境检查、旧版本处理、读取安装数据、解压、写入文件、更新缓存、注册应用、快捷方式和结果验证；完成页显示耗时，日志保存在安装目录的 `install.log`。

同磁盘、当前用户、目标目录为空时直接移动已解压目录，减少重复文件写入；其他情况保留原有复制和重试流程。普通安装完成后以异步方式启动程序。

启动界面按真实进度展示主界面、智能体与会话、工作空间恢复三个阶段。移除固定等待，模型配置先从本地加载，联网刷新延后；失败时显示具体原因并支持重试。修复启动期间关闭窗口导致隐藏进程残留的问题。

本机三次独立配置测试中，智能体就绪中位数由 3.57 秒降至 2.21 秒；实际主窗口约 1 秒出现。安装文件数量从 13,042 减至 7,249。同磁盘测试的程序文件写入阶段由约 40 秒缩短至不足 1 秒。结果受硬盘、系统负载、缓存和安全软件影响。

Windows x64 安装包、自动更新清单 `latest.yml` 和差分更新文件 `blockmap` 一起发布。产物经过哈希、全部差分块、版本及源文件一致性校验，并在项目目录外验证内置智能体、终端、Office、Worker/WASM 和命令行工具。
